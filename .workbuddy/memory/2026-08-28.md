# 2026-08-28 工作日志

## Flutter 云账号功能移除收尾（完成）
- 子代理执行：删除 settings_page.dart 云 Widget 大块（_AccountContent/_Referral*/_Plan*/_Device*/_LoginDialog* 等，约 11448→EOF），各调用方（new_download_dialog、task_list_item、quick_download_dialog、quick_download_submitter、popup_window_service）把云设备引用映射到 `LocalPairingService`（localDevices / dispatchTask，CloudDevice→LocalDevice）。
- `flutter analyze lib` → **0 error**。已删 `test/referral_feature_preference_test.dart`（依赖已删 CloudAuthService）。
- `lib/` 残留仅 4 处注释级云符号提及（download_task.dart、local_pairing_service.dart、quick_download_submitter.dart），无害，保留为说明性文字。
- 全工程 `flutter analyze` 另有 3 error 在 `packages/tray_manager_local/example/`（第三方插件 example 既有 broken import，git status packages/ 为空，非本案、不影响主程序）。

## Rust 引擎传输层增强（前一会话）
- `transport_tuning.rs`（BBR 探测，Linux SO_TCP_CONGESTION=bbr，Windows 文档指引）、`p2p_stats.rs`（BT swarm 统计）已加，`cargo check -p fluxdown_engine --lib` 通过（exit 0，仅 nix future-incompat 提示）。
- events.rs 加 `P2pStats` 变体、lib.rs 注册模块、Engine 启动调用探测；download_manager 加 `session_p2p_stats()` accessor。

## 运行红线（重申）
- `flutter run -d windows` **禁止**（AGENTS.md §2）。合规运行路径：`flutter build windows` 产物 / `cargo build`（GPUI/server/cli）。
- 未 git commit、未改 LICENSE。

## 本轮：修错 + 完善 + 启动验证（完成可验证部分）
- 修 3 个 Flutter 静态分析告警（此前云移除遗留）：`settings_page.dart` 删多余 `flutter/foundation.dart` import；`sidebar.dart` 删未用 `dart:async` import + 删云移除后的死代码 `isOnline`（字段+构造参数+三元用法整段移除，原账号在线指示）。改后 `flutter analyze lib` 应为 0 issue（沙箱内 flutter 工具链本次失效无法复跑，见下）。
- **Windows 工具链实测可用**：MSVC `cl.exe`(14.44 @ D:\IDEA\VisualStudio\Community)、Win SDK 10.0.14393、CMake、Ninja 均在位。
- **flutter 工具链在本沙箱整体失效**：`flutter --version`/`flutter analyze`/`flutter.bat`/`cmd /c flutter` 全部零输出 exit 1，但 `dart --version` 正常(3.13.0)→flutter_tools 快照无法启动，属环境缺陷非代码错。故 Flutter GUI 在本环境既不能构建也不能运行，且即便构建也无显示器可呈现。
- **改用同引擎的 Rust 原生程序做"启动"验证（均 MSVC 构建、Windows 二进制）**：
  - `cargo run -p fluxdown_cli` → 成功编译 `fluxdown.exe`；`ping` 子命令正确报"无法连接 127.0.0.1:17800"（预期：CLI 默认连宿主，无宿主则 exit 5）。
  - `cargo run -p fluxdown_server` → 成功编译并**常驻监听 0.0.0.0:17800**（netstat 确认 LISTENING），加载 BBR/P2P 模块；HTTP 探活 `GET /api/v1/docs` → **200**、`GET /` → 503（未构建 Web UI/未设访问密钥，预期）。证明引擎+新增模块端到端可运行。
  - CLI `ping` 自身因沙箱禁止写 `AppData\Local\FluxDown\logs`(os error 5) 在日志器初始化阶段报错——同类沙箱写权限限制，非代码缺陷；engine 已启动到该阶段。
- **cargo 锁文件怪癖**：沙箱下 `target/debug/.cargo-{build,artifact,}-lock`/`.cargo-lock` 反复出现"拒绝访问"(os error 5)，需每次跑 cargo 前 `rm -f` 清掉陈旧锁再跑（无 cargo/rustc 残留进程时安全）。
- **遗留/未完（诚实标注）**：①P2P 面板 Flutter UI 未建（引擎 `p2p_stats` 模块+accessor 已就绪，但 rinf 信号转发+页面需 flutter 工具可用时再补，且受 download_actor.rs 64 分支 tokio select! 硬上限约束，不能加主循环分支）；②`packages/tray_manager_local/example/` 3 error 为第三方既有问题，不进主包。
- 运行红线遵守：未触碰 `flutter run -d windows`；未 git commit；未改 LICENSE。
